import { CLIOptions } from "../cli";
import { LaravelModelType } from "../types";
export declare const createSource: (filename: string, modelData: LaravelModelType[], enums: string[], options: CLIOptions) => string;
